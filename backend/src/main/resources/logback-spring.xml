<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Import Spring Boot’s base logging configuration -->
    <include resource="org/springframework/boot/logging/logback/base.xml"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
